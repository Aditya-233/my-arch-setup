# -----------------------------------------------------
# CORE SETTINGS
# -----------------------------------------------------
anchor=top-right
default-timeout=5000
width=400
height=200
margin=20
padding=15
border-size=2
border-radius=8
icons=1
max-icon-size=48
font=JetBrainsMono Nerd Font 12

# -----------------------------------------------------
# COLORS (Matches Waybar/Hyprland)
# -----------------------------------------------------
background-color=#1e1e2eE6
text-color=#cdd6f4
border-color=#89b4fa
progress-color=#313244

# -----------------------------------------------------
# BINDINGS (Mouse Clicks)
# -----------------------------------------------------
# Left click to dismiss, Right click to dismiss all
on-button-left=dismiss
on-button-right=dismiss-all
on-touch=dismiss

# -----------------------------------------------------
# APP SPECIFIC RULES
# -----------------------------------------------------
[urgency=critical]
border-color=#f38ba8
default-timeout=0

# -----------------------------------------------------
# MODES
# -----------------------------------------------------
[mode=do-not-disturb]
invisible=1

[mode=do-not-disturb app-name=notify-send]
invisible=0

# -----------------------------------------------------
# INTERACTIVE NOTIFICATIONS
# -----------------------------------------------------
# Replaced custom scripts with standard commands

[summary~="Setup Wi-Fi"]
on-button-left=exec nm-connection-editor; dismiss

[summary~="Update System"]
on-button-left=exec kitty --title "System Update" sh -c "yay -Syu"; dismiss
